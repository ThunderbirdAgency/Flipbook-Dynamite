'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import Link from 'next/link';
export default function JoinTeam(){const router=useRouter();const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <main className="min-h-screen bg-slate-950 px-6 py-24 text-white"><div className="mx-auto max-w-lg rounded-2xl border border-slate-700 p-8"><h1 className="text-2xl font-semibold">Join your Flipbook Dynamite team</h1><p className="my-5 text-slate-400">Sign in with the email this invitation was created for, then accept. You’ll gain access to this team’s publications. Your own workspace stays separate.</p><Link className="text-cyan-300 underline" href="/sign-in" target="_blank">Sign in in another tab</Link><button disabled={busy} className="mt-5 block rounded-lg bg-cyan-400 px-5 py-3 font-semibold text-slate-950 disabled:opacity-40" onClick={async()=>{setBusy(true);setError('');try{const token=location.hash.slice(1);if(!token)throw new Error('Open the complete invitation link provided by your team.');const r=await fetch('/api/team',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'accept',token})});const data=await r.json();if(!r.ok)throw new Error(data.error);history.replaceState(null,'','/team/join');router.push('/app');router.refresh();}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}>{busy?'Joining…':'Accept invitation'}</button>{error&&<p role="alert" className="mt-4 text-red-300">{error}</p>}</div></main>;}
