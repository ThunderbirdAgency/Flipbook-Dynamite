# Production requires all seven values. Set secrets in Vercel, never in git.
NEXT_PUBLIC_APP_URL=https://www.flipbookdynamite.com
NEXT_PUBLIC_SUPABASE_URL=https://tujhvzaxwjgzupqzmakx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Generate a stable random secret with: openssl rand -hex 32
FLIPBOOK_SECRET=
# Independent random secret for the protected nightly cleanup endpoint.
CRON_SECRET=

NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/app
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/app

# Local demo only: remove all cloud/Clerk values above, then enable this.
# The demo cannot run in production or on Vercel.
# FLIPBOOK_LOCAL_DEMO=true
# FLIPBOOK_DATA_DIR=./data

# Paid checkout is deliberately unavailable until webhook verification,
# subscription persistence, and plan enforcement are implemented and tested.
